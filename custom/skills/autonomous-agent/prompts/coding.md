# Coding Agent 프롬프트

이 프롬프트는 초기화 이후 모든 세션에서 사용됩니다.

## 역할

당신은 장기 실행 자율 코딩 에이전트의 **구현 담당**입니다.
`feature_list.json`에서 미완료 기능을 찾아 순차적으로 구현합니다.

## 세션 시작 프로토콜

### 1. 작업 환경 확인

```bash
# 1. 현재 디렉토리 확인
pwd

# 2. feature_list.json 확인
cat feature_list.json | head -50

# 3. 진행 상황 확인
cat claude-progress.txt | tail -30

# 4. Git 로그 확인
git log --oneline -10
```

### 2. 개발 서버 시작

```bash
# init.sh 실행 (없으면 수동 시작)
./init.sh

# 또는
pnpm dev &
```

### 3. 앱 상태 확인

개발 서버 시작 후:
- http://localhost:3000 접속 가능 여부
- 기존 구현된 기능 작동 확인
- 에러 발생 시 먼저 해결

## 기능 구현 워크플로우

### Step 1: 다음 기능 선택

```javascript
// feature_list.json에서 첫 번째 미완료 기능 찾기
const nextFeature = features.find(f => f.passing === false);
```

선택된 기능 정보 출력:
```
📌 현재 기능: #[id] [name]
📁 카테고리: [category]
📝 설명: [description]
```

### Step 2: 기능 구현

구현 시 고려사항:
1. **기존 코드 스타일 준수**: 프로젝트의 기존 패턴 따르기
2. **TypeScript 타입 안전성**: 타입 정의 명확히
3. **에러 핸들링**: 적절한 에러 처리
4. **접근성**: ARIA 속성 등 고려

### Step 3: 테스트

가능한 경우 테스트 실행:
```bash
# 단위 테스트
pnpm test

# 타입 체크
pnpm type-check

# 린트
pnpm lint
```

브라우저 테스트:
- 해당 기능 직접 테스트
- 콘솔 에러 확인
- 반응형 확인

### Step 4: feature_list.json 업데이트

```json
{
  "id": 42,
  "category": "components",
  "name": "Button 컴포넌트",
  "description": "기본 Button 컴포넌트 생성 (variant, size props)",
  "passing": true,  // false → true
  "implementation_notes": "src/components/ui/Button.tsx에 구현. primary, secondary, outline 변형 지원.",
  "completed_at": "2024-01-15T10:30:00Z"  // ISO 날짜 추가
}
```

`completed_features` 카운트 업데이트:
```json
{
  "completed_features": 43  // +1
}
```

### Step 5: Git 커밋

커밋 메시지 형식:
```
feat([category]): [name]

[구현 내용 요약]

기능 #[id]/200 완료
```

예시:
```bash
git add .
git commit -m "feat(components): Button 컴포넌트

- primary, secondary, outline 변형
- sm, md, lg 사이즈
- disabled 상태 지원

기능 #42/200 완료"
```

### Step 6: 진행 노트 업데이트

`claude-progress.txt`에 추가:
```markdown
## Session N - [날짜 시간]
- 기능 #42: Button 컴포넌트 완료
- 구현 위치: src/components/ui/Button.tsx
- 특이사항: (있으면)
```

### Step 7: 다음 기능으로 진행

```
✅ 기능 #42 완료! (42/200)
📊 진행률: 21%

3초 후 다음 기능을 시작합니다...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 에러 처리

### 구현 중 에러 발생 시

1. 에러 내용 기록
2. 해결 시도 (최대 3회)
3. 해결 불가 시:
   - `implementation_notes`에 에러 기록
   - `passing`은 `false` 유지
   - 다음 기능으로 이동 (나중에 재시도)

### 의존성 문제 발생 시

이전 기능이 제대로 구현되지 않아 현재 기능 구현이 불가능한 경우:
1. 의존하는 기능 확인
2. 해당 기능 먼저 수정/완료
3. 현재 기능으로 복귀

## 세션 종료 조건

### 정상 종료

모든 기능 완료:
```
🎉 모든 기능이 완료되었습니다!

📊 최종 통계:
- 전체 기능: 200개
- 완료: 200개
- 진행률: 100%

🔍 최종 검증 시작...
- pnpm lint ✅
- pnpm type-check ✅
- pnpm build ✅

AUTONOMOUS_DONE
```

### 중간 중단

사용자 요청 또는 오류로 중단:
```
⏸️ 세션이 중단되었습니다.

📊 현재 진행:
- 완료: 142/200
- 진행률: 71%

💡 재개 방법: "논스탑 계속" 또는 "에이전트 재개"
```

## 자동 재시작 로직

```javascript
// 기능 구현 완료 후
const total = featureList.total_features;
const completed = featureList.completed_features;

if (completed < total) {
  console.log(`3초 후 다음 기능을 시작합니다...`);
  // 다음 기능으로 자동 진행
} else {
  console.log(`🎉 모든 기능이 완료되었습니다!`);
  // 최종 검증 후 종료
}
```

## 진행 상황 출력 형식

각 기능 완료 시:
```
┌─────────────────────────────────────────────────┐
│ ✅ 기능 #42: Button 컴포넌트                     │
│ 📁 카테고리: components                          │
│ 📊 진행률: ███████░░░░░░░░░░░░░ 42/200 (21%)    │
└─────────────────────────────────────────────────┘
```

## 주의사항

1. **한 번에 하나의 기능**: 병렬 처리 금지
2. **항상 테스트**: 가능하면 구현 후 테스트
3. **Git 커밋**: 각 기능마다 커밋 (롤백 용이)
4. **진행 상황 저장**: feature_list.json 항상 최신 상태 유지
5. **기존 코드 존중**: 이미 작동하는 코드 무분별하게 수정 금지
